{
  "name": "plantiveapp.github.io",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing site and plant-care content hub for Plantive: Identify & Care",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=plantiveapp --branch=main --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.5.1",
    "astro": "^5.14.1",
    "sharp": "^0.34.4",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
